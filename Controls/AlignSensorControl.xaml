<UserControl x:Class="UaaSolutionWpf.Controls.AlignSensorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UaaSolutionWpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="400" FontSize="16">
    <Border BorderBrush="Gray" BorderThickness="1" Background="#F0F0F0">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto" MinWidth="87"/>
            </Grid.ColumnDefinitions>

            <!-- Align Sensor and ComboBox -->
            <TextBlock Text="Align Sensor" 
                     Grid.Row="0" Grid.Column="0" 
                     Margin="5,5,5,2"/>

            <ComboBox Grid.Row="1" Grid.Column="0"
                      Margin="5,0,5,5"/>

            <!-- Target section -->
            <TextBlock Text="Target" 
                     Grid.Row="0" Grid.Column="1"
                     Margin="5,5,5,2"/>

            <TextBox Grid.Row="1" Grid.Column="1"
                     Margin="5,0,5,5"/>

            <!-- Actual section -->
            <TextBlock Text="Actual" 
                     Grid.Row="2" Grid.Column="0"
                     Margin="5,5,5,2"/>

            <TextBox Grid.Row="2" Grid.Column="1"
                    Margin="5,5,5,5" FontSize="48"/>

            <TextBlock Text="na" 
                     Grid.Row="2" Grid.Column="2"
                     FontSize="24"
                     Margin="5,0,5,0"
                     VerticalAlignment="Center" Height="32"/>
        </Grid>
    </Border>
</UserControl>