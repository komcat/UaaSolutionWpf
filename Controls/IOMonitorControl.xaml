<UserControl x:Class="UaaSolutionWpf.Controls.IOMonitorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:UaaSolutionWpf.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">

    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <Style x:Key="StatusIndicatorStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="StrokeThickness" Value="1"/>
            <Setter Property="Stroke" Value="DarkGray"/>
        </Style>
        <Style x:Key="IOButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel>
                <TextBlock x:Name="DeviceNameText" Text="Device Name" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="IpAddressText" Text="IP: 192.168.0.1" FontSize="14" Margin="0,5,0,0"/>
                <TextBlock x:Name="ConnectionStatusText" Text="Status: Connected" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Tab Control for Input/Output -->
        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Outputs">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                    <ItemsControl x:Name="OutputList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,2" Padding="5" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding PinNumber}" 
                                                 VerticalAlignment="Center" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                                 VerticalAlignment="Center"/>
                                        <Ellipse Grid.Column="2" Style="{StaticResource StatusIndicatorStyle}"
                                                Fill="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
                                        <Button Grid.Column="3" Content="{Binding ButtonText}" 
                                                Command="{Binding ToggleCommand}"
                                                Style="{StaticResource IOButtonStyle}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Inputs">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                    <ItemsControl x:Name="InputList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,2" Padding="5" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding PinNumber}" 
                                                 VerticalAlignment="Center" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                                 VerticalAlignment="Center"/>
                                        <Ellipse Grid.Column="2" Style="{StaticResource StatusIndicatorStyle}"
                                                Fill="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>